/**
 * Типи для компонента повідомлення про помилку
 */

/**
 * Пропси для компонента повідомлення про помилку
 */
export interface ErrorMessageProps {
  /**
   * Текст повідомлення про помилку
   */
  message: string;
  
  /**
   * Функція, яка викликається при натисканні на кнопку "Спробувати знову"
   */
  onRetry?: () => void;
}
